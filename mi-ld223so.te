
module mi-ld223so 1.0;

require {
	type user_home_t;
	type snappy_t;
	class dir { getattr open read search };
	class file { map open read };
}

#============= snappy_t ==============

#!!!! This avc is allowed in the current policy
allow snappy_t user_home_t:dir { getattr open read search };

#!!!! This avc is allowed in the current policy
allow snappy_t user_home_t:file { map open read };
